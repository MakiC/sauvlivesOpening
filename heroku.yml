build:
  docker:
    web: Dockerfile
  config:
    NODE_ENV: development
    DB_HOST: db  # Make sure these match your Docker Compose environment variables
    DB_USER: root
    DB_PASSWORD: test
    DB_NAME: openingDayAppDb

run:
  web: docker-compose up
